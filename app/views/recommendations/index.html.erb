<div class="row justify-content-center">
  <div class="col-sm-8 my-3">

  </div>
</div>
<div class="container">
  <% @recommendations.each do |recommendation| %>
    <h1>Name: <%= recommendation.name %></h1>
    <h4>Description: <%= recommendation.description %></h4>
    <p>Price: <%= recommendation.price %></p>
    <%= link_to "click more", recommendation_path(recommendation) %>
  <% end %>
</div>
